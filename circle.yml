dependencies:
  pre:
    - script/ci/prepare.sh
  cache_directories:
    - ~/dependencies
    - ~/.mix
    - _build
    - deps

test:
  override:
    - script/ci/tests.sh

deployment:
  staging:
    branch: master
    heroku:
      appname: stockholm-elixir
